(()=>{'use strict';
const $=s=>document.querySelector(s);const esc=s=>String(s??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const n=(v,d=1)=>v==null||!Number.isFinite(+v)?'—':(+v).toFixed(d);const pct=v=>v==null||!Number.isFinite(+v)?'—':`${+v>=0?'+':''}${n(v,2)}%`;
async function get(){const r=await fetch(`pre-trade-evidence.json?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(r.status);return r.json()}
function ensure(){if($('#preTradeEvidence'))return;const anchor=$('#firstTradeProtocol')||$('#botOpsMaturity')||document.querySelector('.hero');if(!anchor)return;const w=document.createElement('div');w.innerHTML=`<section class="card" id="preTradeEvidence"><div class="card-head"><div><div class="card-title">Pre-first-trade evidence</div><div class="card-meta" id="pteMeta">Loading research context…</div></div><span class="badge" id="pteBadge">RESEARCH ONLY</span></div><div class="grid4" id="pteGrid"><div class="empty">Loading…</div></div><div class="grid2" style="margin-top:14px"><div><div class="card-title" style="font-size:14px">Current setup percentiles</div><div class="analysis-list" id="ptePct"></div></div><div><div class="card-title" style="font-size:14px">Historical context</div><div class="analysis-list" id="pteHist"></div></div></div><p class="card-meta" style="margin-top:12px">Nearest historical setups, MAE/MFE and historical win rates are descriptive context only. They are not a probability forecast and cannot place an order.</p></section>`;const node=w.firstChild;anchor.parentNode.insertBefore(node,anchor.nextSibling)}
function render(x){ensure();$('#pteMeta').textContent=`${x.mode||'PRE-TRADE RESEARCH'} · ${x.readiness_state||'—'}`;$('#pteGrid').innerHTML=[['Historical V3 trades',x.historical_trade_count??'—'],['Similar setups',x.similar_setup_count??'—'],['Similar avg',pct(x.similar_avg_net_pct)],['Similar win rate',x.similar_win_rate_pct==null?'—':`${n(x.similar_win_rate_pct,1)}%`],['Median MAE',pct(x.historical_mae_median_pct)],['Median MFE',pct(x.historical_mfe_median_pct)],['Rehearsal',x.rehearsal_state||'—'],['Trade #2 lock',x.trade_two_locked?'LOCKED':'REVIEWED']].map(([a,b])=>`<div class="metric"><div class="metric-label">${esc(a)}</div><div class="metric-value">${esc(b)}</div></div>`).join('');const p=x.setup_percentiles||{};$('#ptePct').innerHTML=['rsi','adx','atr_pct','ema_spread_pct'].map(k=>`<div class="analysis-row"><span class="analysis-label">${esc(k.replaceAll('_',' ').toUpperCase())}</span><strong>${p[k]==null?'—':`${n(p[k],1)}th`}</strong></div>`).join('');$('#pteHist').innerHTML=`<div class="analysis-row"><span class="analysis-label">Packet prepared</span><strong>${x.packet_exists?'YES':'NO'}</strong></div><div class="analysis-row"><span class="analysis-label">Packet expiry</span><strong>${x.packet_expires_at?new Date(x.packet_expires_at).toLocaleString():'—'}</strong></div><div class="analysis-row"><span class="analysis-label">Review state</span><strong>${esc(x.review_state||'—')}</strong></div><div class="analysis-row"><span class="analysis-label">Order controls</span><strong class="up">NONE ✓</strong></div>`}
async function load(){ensure();try{render(await get())}catch(e){$('#pteMeta').textContent='Pre-trade evidence feed not published yet';$('#pteGrid').innerHTML='<div class="empty">The private evidence engine is collecting. No execution state is inferred from a missing research feed.</div>'}}
load();setInterval(load,60000);
})();
