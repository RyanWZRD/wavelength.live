(()=>{'use strict';
const $=s=>document.querySelector(s);const esc=s=>String(s??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const human=v=>String(v??'UNKNOWN').replaceAll('_',' ').toLowerCase().replace(/^./,c=>c.toUpperCase());
const pct=v=>typeof v==='number'?`${v>=0?'+':''}${v.toFixed(2)}%`:'—';
const row=(a,b)=>`<div class="analysis-row"><span class="analysis-label">${esc(a)}</span><strong>${esc(b)}</strong></div>`;
async function get(){const r=await fetch(`candidate-v3-forward-evidence.json?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(r.status);return r.json()}
function ensure(){if($('#candidateV3ForwardEvidence'))return;const anchor=$('#researchIntelligenceV2')||$('#researchIntelligence')||document.querySelector('.hero');if(!anchor)return;const s=document.createElement('section');s.className='card';s.id='candidateV3ForwardEvidence';s.innerHTML=`<div class="card-head"><div><div class="card-title">Candidate V3 genuine forward evidence</div><div class="card-meta" id="v3fwdMeta">Loading canonical forward-shadow evidence…</div></div><span class="badge warn" id="v3fwdBadge">READ ONLY</span></div><div class="grid4" id="v3fwdGrid"></div><div class="grid2" style="margin-top:14px"><div><div class="card-title" style="font-size:14px">Evidence lineage</div><div id="v3fwdLineage" class="analysis-list"></div></div><div><div class="card-title" style="font-size:14px">Latest genuine episodes</div><div id="v3fwdEvents" class="analysis-list"></div></div></div><p class="card-meta" style="margin-top:12px">Only durable V3 lifecycle <strong>WOULD_ENTER_NEXT_OPEN</strong> episodes count as genuine forward entries. Repeated raw indicator snapshots do not count. This is validation evidence, not trade permission; orders and live money remain disabled.</p>`;anchor.parentNode.insertBefore(s,anchor.nextSibling)}
function eventRows(events){if(!events.length)return row('Episodes','Waiting for first genuine V3 entry');return events.slice(-5).reverse().map(e=>{const o=e.outcomes||{},latest=o['24h']||o['12h']||o['4h']||{};const bits=[e.signal_bar_time?new Date(e.signal_bar_time).toLocaleString():'—',latest.return_pct==null?'pending':pct(latest.return_pct),latest.mfe_pct==null?'':`MFE ${pct(latest.mfe_pct)}`,latest.mae_pct==null?'':`MAE ${pct(latest.mae_pct)}`].filter(Boolean).join(' · ');return row(e.event_id||'V3 event',bits)}).join('')}
function render(x){ensure();const s=x.summary||{},m=x.methodology||{},c=s.resolved_counts||{};$('#v3fwdMeta').textContent=`${human(s.evidence_state)} · ${x.generated_at?new Date(x.generated_at).toLocaleString():'—'}`;$('#v3fwdBadge').textContent=x.orders_enabled?'UNSAFE':'NO ORDERS';$('#v3fwdGrid').innerHTML=[['Genuine entries',s.genuine_forward_entry_events??0],['4h resolved',c['4h']??0],['12h resolved',c['12h']??0],['24h resolved',c['24h']??0],['3d resolved',c['3d']??0],['7d resolved',c['7d']??0],['Latest shadow',human(s.latest_shadow_action)],['Entry signal',s.latest_shadow_entry_signal?'YES':'NO']].map(([a,b])=>`<div class="metric"><div class="metric-label">${esc(a)}</div><div class="metric-value">${esc(b)}</div></div>`).join('');$('#v3fwdLineage').innerHTML=row('Strategy',`${x.strategy_id||'candidate_v3'} · ${x.strategy_version||'—'}`)+row('Canonical event source',m.canonical_event_source||'—')+row('Raw snapshot events counted',m.raw_snapshot_entry_signals_are_not_counted_as_independent_forward_events?'NO':'UNKNOWN')+row('Retrospective backfill',m.retrospective_pre_start_backfill?'YES':'NO')+row('Entry reference',m.entry_reference||'—')+row('Outcome source',m.outcome_source||'—')+row('MFE / MAE',m.mfe_mae_enabled?'ON':'OFF')+row('Fixed-stop touches',m.fixed_stop_touch_tracking?'ON':'OFF')+row('Orders',x.orders_enabled?'ON':'OFF')+row('Live money',x.live_money_enabled?'ON':'OFF');$('#v3fwdEvents').innerHTML=eventRows(x.events||[])}
async function load(){ensure();try{render(await get())}catch(e){$('#v3fwdMeta').textContent='Candidate V3 forward-evidence feed unavailable';$('#v3fwdGrid').innerHTML='<div class="empty">A missing feed is not a signal and does not imply trading permission.</div>'}}
load();setInterval(load,60000);
})();
