(()=>{
'use strict';
const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const n=(v,d=1)=>v==null||!Number.isFinite(+v)?'—':(+v).toLocaleString('en-GB',{maximumFractionDigits:d,minimumFractionDigits:d});
const pct=v=>v==null||!Number.isFinite(+v)?'—':`${+v>=0?'+':''}${n(+v,2)}%`;
const fmtTime=v=>{if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleString('en-GB',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})};
const ageHours=v=>{if(!v)return null;const t=new Date(v).getTime();return Number.isFinite(t)?Math.max(0,(Date.now()-t)/36e5):null};
function freshness(name,ts,expectedHours,transport){const age=ageHours(ts),degraded=age==null||age>expectedHours;return `<article class="validation-card metric"><div class="validation-top"><div><div class="strategy-name">${esc(name)}</div><div class="card-meta">Latest bar ${fmtTime(ts)}${transport?` · ${esc(transport)}`:''}</div></div><span class="badge ${degraded?'bad':'good'}">${degraded?'DEGRADED':'HEALTHY'}</span></div><div class="grid4"><div><div class="metric-label">Age</div><div class="metric-value">${age==null?'—':n(age,1)+'h'}</div></div><div><div class="metric-label">Limit</div><div class="metric-value">${n(expectedHours,1)}h</div></div></div></article>`;}
async function load(){const host=$('#ensembleForwardHealth');if(!host)return;try{const r=await fetch(`ensemble-forward-observer.json?t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(String(r.status));const d=await r.json(),s=d.source_states||{},p=d.portfolio||{},c=d.coverage||{},rf=d.regime_data_freshness||{},v3=s.candidate_v3||{},cr=s.crowding_v2||{},re=s.regime_ensemble||{},observerAge=ageHours(d.generated_at),observerHealthy=observerAge!=null&&observerAge<=8.5;host.innerHTML=`<div class="validation-card metric" style="grid-column:1/-1"><div class="validation-top"><div><div class="strategy-name">Frozen ensemble observer</div><div class="card-meta">Started ${fmtTime(d.forward_start)} · updated ${fmtTime(d.generated_at)} · fixed one-third sleeves</div></div><span class="badge ${observerHealthy?'good':'bad'}">${observerHealthy?'OBSERVER HEALTHY':'OBSERVER STALE'}</span></div><div class="grid4"><div><div class="metric-label">Status</div><div class="metric-value" style="font-size:13px">${esc(String(d.status||'UNKNOWN').replaceAll('_',' '))}</div></div><div><div class="metric-label">Forward events</div><div class="metric-value">${c.genuine_forward_entry_events||0}</div></div><div><div class="metric-label">Return</div><div class="metric-value">${pct(p.return_pct)}</div></div><div><div class="metric-label">Drawdown</div><div class="metric-value">${pct(p.closed_event_max_dd_pct)}</div></div></div><div class="gate-list">Research-only · PAPER/live eligibility: ${d.paper_or_live_eligible?'YES':'NO'} · No performance weighting · No agreement gate</div></div>${freshness('Candidate V3',v3.latest_shadow_bar,8.5,'4h shadow feed')}${freshness('Crowding v2',cr.latest_shadow_bar,8.5,'4h shadow feed')}${freshness('Regime ensemble',rf.latest_market_bar||re.latest_market_bar,40,'Binance daily archive')}`;}catch(e){host.innerHTML='<div class="empty">Ensemble forward-health feed unavailable.</div>'}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',load);else load();setInterval(load,5*60*1000);
})();
(()=>{const boot=()=>{if(document.querySelector('script[data-crypto-expansion]'))return;const s=document.createElement('script');s.src='crypto-research-expansion.js?v=20260910-1';s.defer=true;s.dataset.cryptoExpansion='1';document.head.appendChild(s)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot()})();
