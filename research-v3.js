(()=>{
'use strict';
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pct=v=>Number.isFinite(+v)?`${+v>=0?'+':''}${(+v).toFixed(2)}%`:'—', money=v=>Number.isFinite(+v)?`${+v>=0?'+':''}$${Math.abs(+v).toFixed(2)}`:'—', cls=v=>+v>0?'up':+v<0?'down':'';
async function j(u){const r=await fetch(`${u}${u.includes('?')?'&':'?'}t=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(u);return r.json()}
const style=document.createElement('style');style.textContent=`.ev-shell{display:grid;gap:16px;margin:18px 0}.ev-grid2{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.ev-card{border:1px solid var(--border,#24303a);border-radius:16px;padding:18px;background:rgba(255,255,255,.012)}.ev-head{display:flex;justify-content:space-between;gap:10px;align-items:flex-start;margin-bottom:12px}.ev-title{font:700 18px 'Bricolage Grotesque',sans-serif}.ev-meta{font:500 12px 'IBM Plex Mono',monospace;opacity:.65}.ev-row{display:grid;grid-template-columns:150px 1fr auto;gap:10px;padding:9px 0;border-bottom:1px solid rgba(255,255,255,.06);align-items:center}.ev-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.ev-kpi{padding:12px;border-radius:12px;background:rgba(255,255,255,.025)}.ev-kpi b{display:block;font:700 18px 'IBM Plex Mono',monospace;margin-top:5px}.ev-warn{border-left:3px solid currentColor;padding-left:10px}.prov{display:inline-block;border:1px solid currentColor;border-radius:999px;padding:3px 7px;font:600 10px 'IBM Plex Mono',monospace;opacity:.8}.smallwarn{padding:9px 11px;border-radius:10px;background:rgba(255,255,255,.035);font-size:12px;margin:8px 0}.life{display:flex;gap:8px;flex-wrap:wrap}.life span{border:1px solid var(--border,#24303a);border-radius:999px;padding:5px 8px;font:600 11px 'IBM Plex Mono',monospace}.digest p{margin:7px 0}.export-btn{cursor:pointer}@media(max-width:900px){.ev-grid2,.ev-kpis{grid-template-columns:1fr}.ev-row{grid-template-columns:100px 1fr}.ev-row>:last-child{grid-column:2}}`;document.head.appendChild(style);
function sampleLabel(n){return n<10?'DESCRIPTIVE ONLY':n<25?'INSUFFICIENT DATA':n<75?'EARLY EVIDENCE':n<200?'EMERGING EVIDENCE':'MORE MEANINGFUL'}
function inject(){if($('#evidenceQuality'))return;const footer=document.querySelector('footer');const s=document.createElement('section');s.id='evidenceQuality';s.className='ev-shell';s.innerHTML=`<div class="ev-grid2"><div class="ev-card"><div class="ev-head"><div><div class="ev-title">Server evidence status</div><div class="ev-meta">24/7 radar history · exact horizon outcomes</div></div><span class="badge warn" id="evStage">LOADING</span></div><div id="evStatus"></div></div><div class="ev-card"><div class="ev-head"><div><div class="ev-title">Data provenance</div><div class="ev-meta">Where each evidence stream comes from</div></div></div><div id="evProvenance"></div></div></div><div class="ev-grid2"><div class="ev-card"><div class="ev-head"><div><div class="ev-title">Opportunity lifecycle</div><div class="ev-meta">Current candidates · trajectory · blockers · correlation</div></div><select class="select" id="lifeCoin"></select></div><div id="lifeView"></div></div><div class="ev-card"><div class="ev-head"><div><div class="ev-title">Score calibration</div><div class="ev-meta">Do higher radar scores actually produce better outcomes?</div></div></div><div id="calibration"></div></div></div><div class="ev-grid2"><div class="ev-card"><div class="ev-head"><div><div class="ev-title">Benchmark attribution</div><div class="ev-meta">Selection vs cash drag vs fees vs market direction</div></div></div><div id="benchmarkAttr"></div></div><div class="ev-card"><div class="ev-head"><div><div class="ev-title">ETH V3 vs wider-market paper</div><div class="ev-meta">Validated ETH shadow evidence beside broad paper research</div></div></div><div id="shadowCompare"></div></div></div><div class="ev-grid2"><div class="ev-card"><div class="ev-head"><div><div class="ev-title">Bootstrap uncertainty</div><div class="ev-meta">Simple resampled expectancy interval · closed trades only</div></div></div><div id="bootstrap"></div></div><div class="ev-card"><div class="ev-head"><div><div class="ev-title">Weekly research digest</div><div class="ev-meta">What changed, what mattered, what still needs evidence</div></div><button class="pill-btn export-btn" id="exportResearch">Export JSON</button></div><div id="digest" class="digest"></div></div></div>`;footer?.before(s)}
function status(e){const n=+e.record_count||0,r=+e.resolved_4h||0;$('#evStage').textContent=e.evidence_stage||sampleLabel(r);$('#evStatus').innerHTML=`<div class="ev-kpis"><div class="ev-kpi"><span class="ev-meta">Server snapshots</span><b>${e.snapshot_count||0}</b></div><div class="ev-kpi"><span class="ev-meta">Radar records</span><b>${n}</b></div><div class="ev-kpi"><span class="ev-meta">Resolved 4h</span><b>${r}</b></div><div class="ev-kpi"><span class="ev-meta">4h false positives</span><b>${Number.isFinite(+e.false_positive_rate_4h)?(+e.false_positive_rate_4h).toFixed(1)+'%':'—'}</b></div></div><div class="smallwarn">${sampleLabel(r)} · conclusions remain provisional until sample size and regime coverage are adequate.</div>`}
function provenance(e,p,r,v){const rows=[['Hourly radar',e.provenance?.radar||'Server-side hourly GitHub Actions','1h'],['Outcome resolver',e.provenance?.outcomes||'Exact historical target candle','EXACT'],['Paper account','Private simulation → sanitized public feed','4h'],['V3 validation','Private shadow → sanitized validation feed','4h'],['Live P&L','Browser display pricing only','~10s']];$('#evProvenance').innerHTML=rows.map(x=>`<div class="ev-row"><strong>${esc(x[0])}</strong><span>${esc(x[1])}</span><span class="prov">${x[2]}</span></div>`).join('')}
function lifecycle(e){const xs=e.trajectories||[],sel=$('#lifeCoin');sel.innerHTML=xs.map(x=>`<option>${esc(x.symbol)}</option>`).join('');const render=()=>{const s=sel.value,x=xs.find(z=>z.symbol===s),b=(e.current_blockers||[]).find(z=>z.symbol===s);if(!x){$('#lifeView').innerHTML='<div class="empty">Collecting server trajectories.</div>';return}const h=x.history||[],first=h[0],last=h[h.length-1],rankMove=first&&last?`${first.rank} → ${last.rank}`:'—';const corr=(b?.correlations||[]).map(c=>`${c.with} ${(+c.correlation).toFixed(2)}`).join(' · ')||'No measured blocker';$('#lifeView').innerHTML=`<div class="ev-kpis"><div class="ev-kpi"><span class="ev-meta">Score</span><b>${x.score}</b></div><div class="ev-kpi"><span class="ev-meta">Direction</span><b>${esc(x.direction)}</b></div><div class="ev-kpi"><span class="ev-meta">Rank path</span><b>${rankMove}</b></div><div class="ev-kpi"><span class="ev-meta">Stage</span><b>${esc(String(x.stage).replaceAll('_',' '))}</b></div></div><div class="life" style="margin-top:12px">${h.map(z=>`<span>${new Date(z.time).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})} · ${z.score}</span>`).join('')}</div><div class="ev-meta" style="margin-top:12px">Observed blockers</div>${(b?.reasons||['Awaiting evidence']).map(q=>`<div class="ev-row"><strong>Gate</strong><span>${esc(q.replaceAll('_',' '))}</span><span></span></div>`).join('')}<div class="ev-meta" style="margin-top:10px">Correlation context: ${esc(corr)}</div>`};sel.onchange=render;render()}
function calibration(e){const bs=e.score_buckets||{},keys=['90-100','80-89','70-79','55-69'];$('#calibration').innerHTML=keys.map(k=>{const x=bs[k]||{},n=x.resolved_4h||0;return `<div class="ev-row"><strong>${k}</strong><span>4h avg ${pct(x.avg_4h)} · win ${Number.isFinite(+x.win_4h)?(+x.win_4h).toFixed(0)+'%':'—'} · 24h avg ${pct(x.avg_24h)}</span><span>${n} resolved</span></div>`}).join('')+`<div class="smallwarn">Calibration only becomes persuasive if higher buckets repeatedly outperform lower buckets across a meaningful sample.</div>`}
async function benchmarkAttr(p,h){const curve=h?.equity_curve||[],start=curve[0]?.equity||p.paper_starting_equity,end=p.paper_equity,total=end-start,fees=+p.fees_paid_total||0,cash=+p.cash||0,eq=+p.paper_equity||1,cashPct=100*cash/eq;$('#benchmarkAttr').innerHTML=`<div class="ev-kpis"><div class="ev-kpi"><span class="ev-meta">Paper change</span><b class="${cls(total)}">${money(total)}</b></div><div class="ev-kpi"><span class="ev-meta">Fees paid</span><b>-$${fees.toFixed(2)}</b></div><div class="ev-kpi"><span class="ev-meta">Cash weight</span><b>${cashPct.toFixed(1)}%</b></div><div class="ev-kpi"><span class="ev-meta">Selection/market residual</span><b>${money(total+fees)}</b></div></div><div class="smallwarn">“Residual” is descriptive only: it mixes market movement, position sizing and selection; it is not causal attribution.</div>`}
function shadowCompare(p,v){const s=v?.strategies?.v3||{},wide=+p.total_return_pct||0;$('#shadowCompare').innerHTML=`<div class="ev-row"><strong>ETH Candidate V3</strong><span>${esc(s.status||'KEEP_WATCHING')} · ${s.completed_trades||0} trades · ${s.observation_days||0} days</span><span>${pct(s.total_return_pct)}</span></div><div class="ev-row"><strong>Wider paper</strong><span>${p.closed_trades_total||0} closed trades · ${p.open_positions?.length||0} open</span><span>${pct(wide)}</span></div><div class="smallwarn">These are different systems and samples. Side-by-side display is for evidence discipline, not a direct performance contest.</div>`}
function bootstrap(p){const t=(p.recent_closed_trades||[]).map(x=>+x.pnl_pct).filter(Number.isFinite);if(t.length<8){$('#bootstrap').innerHTML=`<div class="smallwarn">${t.length} closed trades. Need at least 8 before even showing a crude bootstrap interval.</div>`;return}const means=[];for(let b=0;b<1000;b++){let s=0;for(let i=0;i<t.length;i++)s+=t[Math.floor(Math.random()*t.length)];means.push(s/t.length)}means.sort((a,b)=>a-b);const lo=means[Math.floor(.025*means.length)],hi=means[Math.floor(.975*means.length)],m=t.reduce((a,b)=>a+b,0)/t.length;$('#bootstrap').innerHTML=`<div class="ev-kpis"><div class="ev-kpi"><span class="ev-meta">Mean/trade</span><b>${pct(m)}</b></div><div class="ev-kpi"><span class="ev-meta">2.5%</span><b>${pct(lo)}</b></div><div class="ev-kpi"><span class="ev-meta">97.5%</span><b>${pct(hi)}</b></div><div class="ev-kpi"><span class="ev-meta">n</span><b>${t.length}</b></div></div><div class="smallwarn">Non-parametric browser bootstrap; descriptive, not a guarantee and unreliable for small or regime-concentrated samples.</div>`}
function digest(e,p,v,l,r){const top=(r.opportunities||[])[0],open=p.open_positions||[],net=open.reduce((a,x)=>a+(+x.unrealized_pnl||0),0),resolved=e.resolved_4h||0;$('#digest').innerHTML=`<p><strong>Market:</strong> ${esc(p.market_regime||'—')}. ${top?`${esc(top.symbol)} leads the hourly radar at ${top.score}/100 (${esc(String(top.stage).replaceAll('_',' '))}).`:'No radar leader available.'}</p><p><strong>Paper:</strong> ${open.length} open positions, ${money(net)} estimated net-if-closed P&L, total return ${pct(p.total_return_pct)}.</p><p><strong>Evidence:</strong> ${resolved} exact 4h radar outcomes resolved; stage ${esc(e.evidence_stage||'INSUFFICIENT_DATA')}.</p><p><strong>Validation:</strong> ETH V3 ${esc(v?.strategies?.v3?.status||'—')}; server learning ${esc(l?.learning_stage||l?.stage||'—')}.</p><p><strong>Watch next:</strong> whether high-score buckets actually outperform lower buckets and whether wider-market paper expectancy remains positive after fees across more regimes.</p>`}
function exportData(all){$('#exportResearch').onclick=()=>{const blob=new Blob([JSON.stringify({schema:'wavelength-research-export-v2',generated_at:new Date().toISOString(),...all},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='wavelength-research-export.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}}
async function main(){inject();try{const [e,p,h,v,l,r]=await Promise.all([j('research-evidence.json'),j('paper-portfolio.json'),j('paper-history.json'),j('validation.json'),j('learning-status.json'),j('opportunity-radar.json')]);status(e);provenance(e,p,r,v);lifecycle(e);calibration(e);benchmarkAttr(p,h);shadowCompare(p,v);bootstrap(p);digest(e,p,v,l,r);exportData({server_evidence:e,paper:p,paper_history:h,validation:v,learning:l,radar:r})}catch(err){console.error(err);$('#evStatus').innerHTML='<div class="empty">Evidence feed unavailable. Existing Research panels remain usable.</div>'}}
window.addEventListener('DOMContentLoaded',main);
})();
